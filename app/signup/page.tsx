import { SignupPage } from '@/components/auth/auth-pages';

export const metadata = {
  title: 'WARP - Sign Up',
  description: 'Create a WARP prototype account',
};

export default function SignupRoute() {
  return <SignupPage />;
}
